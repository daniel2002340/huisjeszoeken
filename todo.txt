Here's the full state of the project — everything that's left, compiled from the build history and a codebase sweep.

⚠️ Critical before go-live

1. Nothing is committed to git. The branch has zero commits; all 29+ files (plus everything untracked) exist only in the working tree. One rm -rf away from losing three days of work. This should be the very first thing you do.
2. The seed profile is fake. "Anna & Tom" with anna@example.com — real profiles for your friends (real emails, budgets, letter vars) need to be created via the dashboard or seed script before flipping anything on.
3. Email sending has never actually run. DRY_RUN has always been true; the Nodemailer/SMTP path is untested. You need a Gmail app password (or Resend key) in .env, one real test send, and the Gmail filter tip from PLAN §5 for each friend. ADMIN_EMAIL is also unset, so health alerts currently only log.
4. The Docker-on-Linux 403 problem is unresolved. Cloudflare blocks Node-on-Linux TLS fingerprints (verified), so the Docker deployment may be dead on arrival on the Mini — the launchd path in DEPLOY.md is the likely reality. This needs a decision when you deploy; try docker compose up once, fall back to launchd.
5. Dev database is full of test data (62 listings, 14 matches with fake statuses). Wipe data/app.db before production, or the first real poll will treat old listings as already-seen.

Known bugs / fragile spots

- kamernet.ts is a dead stub — it throws "not implemented", isn't registered in the scheduler, and isn't in SOURCES.md. Either implement it or delete it (only code-level TODO left).
- huurwoningen.com (#2) was built as huurwoningen.nl — assumed same platform; never verified whether .com has separate inventory.
- Cross-source dedupe misses the pararius↔huurwoningen pair — huurwoningen cards lack house numbers, so the same flat alerts twice. The fix I offered (fetch the detail page once for the full address) was never requested. Same applies to all no-house-number sources (huurstunt, huurwoningportaal, directwonen, huure, trovit, rent.nl, marktplaats) — duplicate alerts are by design (over-send), but they'll be noticeable with 23 sources.
- trovit URLs are session-bound redirects that embed your home IP — links  the IP to recipients.
- rent.nl alerts link to their signup page (only link that exists).
- bedrooms are a kamers−1 estimate on ~half the sources (documented, but a by a min-bedrooms profile).
- buurtje's API needs Referer headers; funda blocks headless but tolerates plain fetch; marktplaats hasn't bot-walled yet — all three could break silently. The zero-listing admin alert is the safety net, which is another reason to set ADMIN_EMAIL.

Deferred features (PLAN.md phase 3 + offers along the way)

- Claude Haiku letter personalization (reference one listing detail).
- Per-profile digest mode (instant vs hourly).
- Friend access to the dashboard (Tailscale invite/Funnel; DASHBOARD_PASSW
- Durable DRY_RUN outbox (data/outbox/ with one file per email) — I offered this, never built.
- Detail-page fetching for full addresses (the dedupe fix above).

Cleanup / tech debt

- pnpm web:typecheck isn't part of any standard check, there's no CI, and redesign (refresh procedure exists: pnpm fixture <source>).
- Port 3000 collides with another service on your dev machine (default unchanged).

Current healthy state for context: 23/23 sources built with 233 passing fixture-based tests, dashboard, scam warnings, letter-suppressed corporation alerts, restart-safe backoff, and cookie jars. My suggested order: commit → real profiles → wipe DB → SM(launchd likely) → watch DRY_RUN for a day → go live.
